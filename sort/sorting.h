#ifndef ALGORITHMS_C_SORTING_H
#define ALGORITHMS_C_SORTING_H

//                                                                        Space        | Memory
void counting_sort(void *ptr, size_t count, size_t size, const int k); // O(count + k) | O(count + k)

#endif
